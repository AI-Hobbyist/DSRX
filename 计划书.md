# DiffSinger / DSRX 量化友好 DiT 可切换后端计划书

> 日期：2026-09-19。基线：Kouon-Project/DSRX，分支 `DSRX_DiT`，提交 `14c2e7f71323f6343b4d59f44129f97316c412f5`。
>
> **状态快照**：P0 配置契约、P1 浮点 DiT/训练链、P2 原生 PyTorch 推理和 P3 浮点 ONNX 兼容链已实现并完成合成回归。P4–P6 的量化、蒸馏和 GGUF 尚未实现；仓库没有 acoustic/variance 训练 checkpoint，因此未进行音质、成品模型包或旧权重数值对照。
>
> **P4–P6 共同前提**：必须先有已经训练好的模型。P4 还需要代表性校准/评价数据；P5 需要质量合格的多步教师；P6 需要浮点 DiT 作为图、数值和量化执行对照。随机权重只能验证工程链，不能完成这些阶段。

## 1. 结论与项目边界

建议在现有 backbone 注册机制中增加 **1D、逐帧、双向注意力的 `dit` 后端**，保持 FastSpeech2 条件编码、音素/音符对齐、扩散或流匹配训练目标、声码器以及用户控制参数不变。

| 决策 | 推荐方案 |
| --- | --- |
| 兼容方式 | 增量新增 `dit`，保留 `wavenet`、`lynxnet`、`lynxnet2` 及旧配置回退逻辑 |
| 声学默认骨干 | 8 层，宽度 384，6 头，head dimension 64，MLP ratio 4，逐帧 token，RoPE |
| 条件注入 | 沿用现有帧级条件，加法投影；全局时间通过逐块 `adaLN-Zero` 调制 |
| 生成目标 | 新模型优先沿用现有 Reflow；同时支持 DDPM 的噪声预测接口 |
| 初始推理默认 | Euler 20 步，即无 CFG 时 20 次骨干评估；8/4/2/1 步作为独立质量实验 |
| 浅扩散 | 保留现有辅助解码器及 `T_start=0.4` 默认；提供独立训练的全深度模型路线 |
| 8GB 微调 | batch size 1、768 帧起步、混合精度、逐块重计算、梯度累积 8；不依赖量化才能训练 |
| LoRA | 先提供浮点底座 LoRA：rank 8、alpha 16、只训练指定骨干线性层的适配器 |
| 量化 | FP16/BF16 基线 → 选择性 INT8 → 选择性 INT4；不预设全模型纯整数 |
| 部署优先级 | 原生 PyTorch → 兼容现有宿主的浮点 ONNX → 独立低比特 ONNX → GGUF + 专用执行器 |
| 单步结论 | **理论可行，但不能只换 DiT 或把 `sampling_steps` 改成 1 就保证质量**；需要专门少步训练/蒸馏 |

**“随时切换”的准确含义**：可以选择旧模型包或 DiT 模型包，或者在创建新实验时选择骨干；同一 DiT 的浮点/量化执行变体也可切换。它不意味着把已有 LYNXNet/WaveNet checkpoint 原地解释为 DiT，更不意味着不同架构间任意复用 LoRA。

**“保留所有功能”的验收定义**：旧模型不回归；新 DiT 必须通过本计划的功能矩阵；现有宿主协议不被强制升级。未验证的能力不得标记为“已兼容”。GGUF、单步模型不能通过删掉不支持的功能冒充完整替代品。

### 1.1 记忆与工作范围

- 已核对永久记忆：优先 CodeGraph；不调用未经明确许可的 Explore 工具/子代理；不擅自修改模型代码或无关配置；阶段完成后记录状态和验证结果。
- 仓库记忆已持续记录 P0–P3 的已核实决策与验证；**不把尚未实现的 P4–P6 设计写成已完成能力**。
- 已遵守 [.github/instructions/copilot.instructions.md](.github/instructions/copilot.instructions.md)。架构先通过 CodeGraph 核查；仅对未返回的具体构造器补充精确读取。
- 只新增本计划，不修改训练、推理、依赖、模型权重，也不提交已有未跟踪目录。

## 2. 当前仓库事实与接入位置

| 现状 / 位置 | 对本计划的约束 |
| --- | --- |
| [骨干注册与工厂](modules/backbones/__init__.py#L7-L20) 已支持三种骨干，由 `build_backbone()` 实例化 | 新增注册项即可接入主要构造链；不另造一套歌声训练系统 |
| [兼容解析](modules/compat.py#L1-L24) 支持根配置、嵌套配置和历史 `diff_decoder_type` | 保留旧字段优先级；仅对新 DiT 参数做严格校验，防止未知参数被过滤后静默失效 |
| [声学模型](modules/toplevel.py#L32-L122) 将 `fs2`、可选 `aux_decoder` 与 DDPM/Reflow 组合 | DiT 仅替换 `denoise_fn` 或 `velocity_fn`，不能吞并上层控制逻辑 |
| [variance 模型](modules/toplevel.py#L125-L309) 有时长、pitch、其他 variance 分支 | pitch 与 multi-variance 可分别选骨干；时长仍走原预测器，不虚构“时长扩散后端” |
| [多参数适配器](modules/fastspeech/param_adaptor.py#L31-L86) 将 repeat bins 分给已启用 variance | 接口必须支持 `num_feats>1`，保持曲线顺序与归一化/裁剪语义 |
| [Reflow 实现](modules/core/reflow.py#L36-L138) 从噪声向数据积分，支持 Euler/RK2/RK4/RK5 | 保留时间方向、缩放及浅扩散起点；DiT 不内置求解器 |
| [LoRA 实现](utils/lora.py#L9-L84) 替换 `nn.Linear` 类层，并按名字正则选层 | 优先使用显式 `nn.Linear`，而不是让主投影藏在不可识别的融合参数中 |
| [训练初始化](basics/base_task.py#L59-L91) 和 [微调加载](basics/base_task.py#L130-L195) 已支持冻结、微调、LoRA | 新增模式必须遵守明确的加载顺序和架构校验，不能靠 `strict=False` 假装成功 |
| [声学 ONNX 导出](deployment/exporters/acoustic_exporter.py#L201-L357) 先 trace 骨干、再 script 求解器，使用 opset 15 | 新增 DiT 必须提供普通算子 attention 导出路径，不能要求旧导出环境拥有 SDPA |
| [ONNX Reflow](deployment/modules/rectified_flow.py#L37-L68) 使用 batch 1、动态长度、Euler 循环 | 保留已有签名；不要把原生全部求解器误写成现有 ONNX 已支持 |
| [原生推理优化](inference/optimization.py#L136-L266) 会替换大 Linear、trace 并 warmup | DiT 需要独立的能力判定，防止覆盖量化层或 LoRA forward、错误冻结 shape 分支 |

### 2.1 需要在实现阶段处理的具体风险

1. **padding 不可通过条件值推断。** [modules/fastspeech/acoustic_encoder.py](modules/fastspeech/acoustic_encoder.py) 会叠加音高、说话人、语言和其他嵌入；padding 区域并不可靠地等于全零。当前骨干三参接口没有显式 mask，全局 attention 必须新增可靠的有效帧信息传递。
2. **旧预热长度不是 DiT 的安全默认。** [推理优化说明](docs/InferenceOptimization.md#L1) 包含 6,000/10,000 帧预热。卷积的安全长度不能直接复用为全局 attention 的默认预热长度。
3. **旧导出与新训练环境要隔离。** [requirements-onnx.txt](requirements-onnx.txt) 固定 `torch==1.13.1`；新训练使用 SDPA 需要 PyTorch 2.x。保留 legacy 导出路线，另建经验证的现代量化导出环境，不能全仓统一升级后宣称兼容。
4. **LoRA 错误不能只打印警告。** 当前训练注入失败会被捕获并警告；DiT 训练前应报告命中层、可训练参数数及基座哈希，异常时中止，避免无意全参训练或训练空适配器。
5. **原生线性替换可能吞掉 LoRA 更新。** `LoRALinear` 是 `nn.Linear` 子类；通用替换逻辑需识别该类型，先正确合并到普通 Linear，或走明确支持适配器的实现。量化层同理。
6. **配置继承需要清理。** [原始声学配置](configs/original/acoustic.yaml#L1) 当前启用了 LoRA 并指定本地基座路径；新 DiT 基础模板必须显式覆盖 `lora.enabled=false` 和空基座，不能继承旧架构 LoRA。
7. 本次 PyYAML 读取发现 [variance 模板](configs/templates/config_variance.yaml#L98-L104) 的 `dropout_rate` 缩进存在解析错误；新模板不能盲目复制。另有旧 LYNXNet2 参数名 `dropout` 与部分 YAML 中 `dropout_rate` 不一致的静默过滤风险。**本次只记录，不修改无关旧配置。**

## 3. 模块接口、切换与 checkpoint 契约

### 3.1 保持三个技术维度独立

1. **模型骨干**：`backbone_type = wavenet | lynxnet | lynxnet2 | dit`。
2. **生成目标与采样器**：`diffusion_type = ddpm | reflow`，以及各自已支持的求解器、深度、步数。
3. **执行与精度**：原生 PyTorch、ONNX、未来 GGML；FP32/FP16/BF16/INT8/INT4。

`dit` 是模型骨干，不等于新的 sampler，也不等于新的文件格式。训练态 Module 保持 `state_dict` 和原模块路径可见；部署态 Adapter 负责精度、导出与运行时差异。不要把训练、量化、求解器全塞进一个大类。

### 3.2 张量契约

沿用当前 backbone 的主要布局：

- 输入 `spec`：`[B, F, M, T]`；声学 `F=1, M=128`；pitch 和 variance 沿用 repeat-bin 编码。
- 输入 `diffusion_step`：规范化为 `[B]`；兼容当前单元素时间张量和必要的 `[B,1]` 输入。Reflow 传入的已经是 `t * time_scale_factor`，不重复乘除 1000。
- 输入 `cond`：`[B, H, T]`，默认 `H=256`，与 DiT 内宽 `D=384` 分离。
- 输出：严格恢复 `[B,F,M,T]`；DDPM 表示噪声预测，Reflow 表示速度预测。输出头不改成图像 DiT 的双倍通道或额外方差预测头。
- 新增可选 `valid_mask`：`[B,T]`，统一约定 **True 表示有效帧**。从 `mel2ph>0` 或已知长度构造，经上层、core、全部 sampler 分支传递；不能从 mel 数值或 `cond!=0` 反推。

旧骨干继续使用三参调用。工厂/Adapter 根据显式能力选择调用方式，而不是依靠捕获任意 `TypeError` 回退。新增 mask 参数默认 `None` 保留外部兼容；DiT 的有 padding 训练必须提供 mask，无 mask 仅表示该请求全部帧有效。

attention 只允许有效 key 被关注；padding query 的残差/输出也清零；loss 继续按有效帧计算。全空请求在入口拒绝或走空输出短路，避免所有 key 都被屏蔽后的 Softmax 非有限值。需要区分 PyTorch SDPA 的布尔允许掩码与旧 `key_padding_mask` 的布尔屏蔽语义。

### 3.3 模型包和切换规则

模型包应记录：骨干类型/版本/完整参数、生成目标、时间约定、归一化区间、浅扩散训练范围、声学规格、字典与说话人/语言映射、精度与量化方式、基座哈希、LoRA 目标层/rank/alpha、宿主能力需求。

- **新实验切换**：声学用根级骨干字段，pitch/variance 用各自嵌套字段；可使用 DiT 声学 + 旧 variance，也可反过来，不强迫三个模型同时迁移。
- **已训练模型切换**：选择匹配模型包；停止接收当前实例的新任务、等待正在执行的请求完成、释放旧 GPU 模型后加载新模型。8GB 档不要求双模型同时常驻。失败时重新加载上一个完整模型包。
- **精度切换**：从原始浮点权重重建量化或浮点实例，不在同一对象上反复就地合并/量化，避免误差累计和 LoRA 重复叠加。
- **旧权重迁移**：仅复用经形状和语义双重核验的 FS2、辅助解码器、嵌入等；DiT 骨干需要训练或蒸馏。迁移报告逐项列出加载/重置/拒绝的键，不能用随机 DiT 冒充成功转换。
- **LoRA 兼容**：适配器必须匹配基座、骨干版本和目标层形状。把 LYNXNet2 适配器直接加载到 DiT 应明确报错。
- **全深度与浅扩散**：在 `[0.4,1]` 训练的模型不能保证从 `t=0` 推理；关闭浅扩散需要覆盖完整时间范围的模型，或额外补训。相同限制适用于改变推理深度。

## 4. DiT 结构：先采用简单、可量化、可导出的方案

### 4.1 默认计算流程

1. 将 mel/重复 bins 的 `F×M` 展平为每帧通道 `C`，得到 `[B,T,C]`；**一帧一 token，patch size=1**。
2. 输入投影 `Linear(C,D)` 与帧级条件投影 `Linear(H,D)` 相加。现有音素、f0、variance、性别/速度、说话人及语言控制已包含在条件中，不重建一套文本模型。
3. 时间经无参数的 256 维 sinusoidal embedding，再经过 `Linear(256,D) → SiLU → Linear(D,D)`。
4. 每块使用两个无仿射参数的 LayerNorm；时间经过 `SiLU → Linear(D,6D)`，产生 attention/MLP 各自的 shift、scale、gate。
5. Attention 为标准双向 MHA：一个显式 `qkv: Linear(D,3D)`、一个 `attn.proj: Linear(D,D)`；Q/K 使用实数形式 1D RoPE。不是 causal，不使用 LLM 的逐 token KV cache。
6. MLP：`Linear(D,4D) → GELU(tanh approximation) → Linear(4D,D)`。
7. 最后无仿射 LayerNorm，时间投影 `Linear(D,2D)` 做 shift/scale，`Linear(D,C)` 输出并还原布局。

默认所有 Linear 含 bias；LayerNorm `eps=1e-6`、`elementwise_affine=false`；RoPE 无可训练参数，base 10000，全部 64 个 head dimensions 参与旋转；无可训练位置表。每块调制投影及末端调制/输出按 adaLN-Zero 原则初始化；明确将输出 bias 同步置零，避免“只有 weight 为零”的假零输出。

### 4.2 为什么这样设计

- 显式 Linear 容易被当前 LoRA、量化工具和 ONNX 识别；保持固定权重的 MatMul，不引入动态生成权重的卷积。
- 条件已经按帧对齐，第一版不增加 cross-attention、VAE、文本语言模型或 Mel patch 压缩。它们会扩大参数、改动对齐语义并增加导出工作。
- 不选 SwiGLU 是为了先降低乘法门控和量化消融变量，不表示 SwiGLU 不可量化。
- adaLN-Zero 有一手 DiT 依据，但**动态调制并不天然量化容易**。第一版保留调制、归一化和输出等敏感路径的浮点精度。
- 不以加入自定义 CUDA kernel 为前提；训练优先 SDPA，导出采用相同数学语义的普通算子分解。
- 没有从 LLM 引入 GQA 的必要：这里无自回归 KV 缓存收益；如未来引入必须重新计算参数量并独立比较质量。

### 4.3 推荐默认参数

以下是**待实现/验证的设计配置**，不是当前仓库已经接受的完整配置文件。旧配置键保持原义，新键必须随实现和 schema 一同落地。

| 参数 | 声学默认 | 声学小型备选 | pitch 默认 | multi-variance 默认 |
| --- | ---: | ---: | ---: | ---: |
| 层数 `num_layers` | 8 | 6 | 4 | 4 |
| 宽度 `num_channels` | 384 | 256 | 256 | 256 |
| 注意力头数 `num_heads` | 6 | 4 | 4 | 4 |
| 每头维度 | 64 | 64 | 64 | 64 |
| `mlp_ratio` | 4 | 4 | 4 | 4 |
| 时间 embedding 维度 | 256 | 256 | 256 | 256 |
| 条件维度 `hidden_size` | 256 | 256 | 256 | 256 |
| 输入总通道 `C=F×M` | 128 | 128 | 64 | 48 |
| attention / MLP dropout | 0 / 0 | 0 / 0 | 0 / 0 | 0 / 0 |
| 位置编码 / patch | RoPE / 1 | RoPE / 1 | RoPE / 1 | RoPE / 1 |

多参数总 bins 继续为 48，启用 1/2/3/4 条曲线时，每条分别为 48/24/16/12 bins。`C` 始终为 48，但输出必须按原始 `F,M` 还原，不能打乱曲线含义。没有启用相应预测分支时不创建对应 DiT。

声学新模型基线继续采用 `diffusion_type=reflow`、`sampling_algorithm=euler`、`sampling_steps=20`、`time_scale_factor=1000`、`use_shallow_diffusion=true`、`T_start=T_start_infer=0.4`。保持现有 mel 规格 44.1kHz / hop 512 / 128 bins，不改用户数据和声码器契约。

**选择 22M 而不是几亿参数的原因**：本仓库已有专门条件编码，目标是 8GB 普通显卡上的微调和部署，而不是复制 F5-TTS 的大规模零样本文本到语音训练。宽度 384 是 64 和 128 的倍数，适合常见矩阵/分组布局；但不是所有 GGUF 量化块尺寸都兼容，见第 10 节。

## 5. 参数量计算及核验

### 5.1 明确计算假设

设层数 $L$、宽度 $D$、MLP ratio $r=4$、输入通道 $C=FM$、条件维度 $H=256$、时间 embedding 维度 $E=256$。按第 4 节的**具体结构**计数，不用模糊的“Transformer 约若干倍 $D^2$”估计。

每块：

$$
P_{\mathrm{attn}}=4D^2+4D,\quad
P_{\mathrm{MLP}}=2rD^2+(r+1)D,\quad
P_{\mathrm{ada}}=6D^2+6D
$$

$$
P_{\mathrm{block}}=(10+2r)D^2+(11+r)D=18D^2+15D
$$

其余部分：

$$
P_{\mathrm{other}}=(CD+D)+(HD+D)+(ED+D+D^2+D)+(2D^2+2D)+(DC+C)
$$

$$
P_{\mathrm{DiT}}=L(18D^2+15D)+3D^2+(2C+H+E+6)D+C
$$

不含可训练位置表、额外 QK norm、cross-attention、额外卷积、CFG 空条件参数；如设计变动，必须重算，不继续引用本表。

### 5.2 默认声学 DiT 明细

| 部分 | 参数量 |
| --- | ---: |
| mel 输入 `128→384` | 49,536 |
| 条件投影 `256→384` | 98,688 |
| 时间 MLP `256→384→384` | 246,528 |
| 8 个 Transformer 块，含各自 adaLN 投影 | 21,279,744 |
| 末端时间调制 `384→768` | 295,680 |
| mel 输出 `384→128` | 49,280 |
| **DiT 骨干合计** | **22,019,456（22.019M）** |

| 变体 | 骨干参数量 |
| --- | ---: |
| 声学默认 8×384 | 22,019,456 |
| 声学小型 6×256 | 7,495,808 |
| pitch 4×256，64 bins | 5,096,000 |
| multi-variance 4×256，总 48 bins | 5,087,792 |

小型模型只是低算力备选，不承诺同等音质。pitch 与 multi-variance 两个骨干同时启用时合计 **10,183,792** 参数；这**不包括** variance 前端、可选 melody encoder、duration predictor 和嵌入，因此不是完整 variance 模型的参数量。

### 5.3 完整声学模型示例

为避免只算 DiT 而忽略现有功能，保留现有 6 层、宽度 512 的 ConvNeXt 辅助解码器，并采用以下明确示例：FS2 宽度 256、4 层、2 头、FFN kernel 3、GELU、RoPE；词表大小 256（**示例，非实际字典统计**）；14 位说话人、2 种语言；开启四种 variance 条件、key shift、speed 和 artifact embedding。

| 部分 | 参数量 | 核验方式 |
| --- | ---: | --- |
| 现有 `FastSpeech2Acoustic` | 5,327,168 | 仓库类实际 CPU 实例化计数 |
| 现有 `ConvNeXtDecoder(256,128,512,6,7)` | 14,008,960 | 仓库类实际 CPU 实例化计数 |
| 拟议 DiT | 22,019,456 | 公式与 PyTorch meta Linear 规格交叉核验 |
| **含浅扩散的完整声学模型** | **41,355,584（41.356M）** | 不含声码器、独立 variance 模型 |
| 不创建辅助解码器的全深度架构 | 27,346,624 | 需要匹配的全深度训练，不是直接关闭旧模型开关 |

FS2 总数包括 64 个当前实现中的非训练 RoPE 参数；本例 FS2 可训练数为 5,327,104。后文训练状态预算把全部参数都计为可训练，是略偏保守的估计。

在同一示例前端和辅助解码器下，现有 6×1024 LYNXNet2 骨干 CPU 计数为 **40,619,136**，完整声学约 **59,955,264**。拟议方案参数更少，**不代表速度或音质必然优于卷积骨干**，尤其长序列 attention 的计算会迅速增加。

实际词表、语言、说话人数变化须重新计数：词表每增加一个 token 增加 256 参数；启用时每增加一个说话人/语言通常增加 256 参数。不开启的条件模块不计入实际模型。声码器必须单列统计，不能并入“DiT 22M”。

### 5.4 LoRA 参数

默认只覆盖每块 `qkv`、attention output、MLP 两个 Linear；不包含时间、adaLN、输入/输出投影及 FS2。对一个 `Linear(d_in,d_out)`，LoRA 参数为 $r_L(d_{in}+d_{out})$。

$$
P_{\mathrm{LoRA/block}}=r_L(4D+2D+5D+5D)=16r_LD
$$

声学默认 $L=8,D=384,r_L=8$，得到 **393,216** 个可训练参数；FP32 权重约 **1.5 MiB**，按每参数 16 bytes 预留训练状态约 **6 MiB**。这不包含底座存储与激活。rank 16 时参数翻倍。

训练前输出完整命中列表。拟议层命名可使目标匹配骨干下的 `blocks.*.attn.qkv`、`blocks.*.attn.proj`、`blocks.*.mlp.fc1`、`blocks.*.mlp.fc2`；DDPM/Reflow 前缀不同，必须分别验证。不要直接使用现有宽泛的 `linear` 并继续套用 393,216 的计数。

### 5.5 消费级小 / 中 / 大档与 B 级参数参考

**本节 B 表示十亿（$10^9$）参数，不是 batch size 或显存 GB。** 以下都是同一拟议结构的容量档位，不是已训练模型或现成 checkpoint。消费级以 8–24GB 独立显存设备为主要目标；**M 档仍为全项目推荐默认，B 级仅为扩展研究参考**。

全部档位固定 `C=128,H=256,E=256`、MLP ratio 4、head dimension 64、patch size 1、RoPE、逐块独立 adaLN-Zero、Linear 含 bias、LayerNorm 无仿射参数；仅调整层数与 DiT 内宽，**不同时扩大 FS2/辅助解码器**。默认仍为 Reflow + Euler 20 NFE，浅扩散配置同第 4.3 节。pitch/variance 优先保留原推荐小骨干，不随声学盲目放大。

| 档位 | 定位 | 层数 $L$ | 宽度 $D$ | 头数 | MLP 隐层 | 精确骨干参数 | 示例完整声学参数 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| **S 小型** | 低资源、快速验证、个人小数据微调 | 6 | 256 | 4 | 1,024 | **7,495,808（7.496M）** | 26,831,936 |
| **M 中型 / 默认** | 8GB 微调、LoRA 与推理的平衡档 | 8 | 384 | 6 | 1,536 | **22,019,456（22.019M）** | 41,355,584 |
| **L 大型** | 较充分数据，12–16GB 消费卡优先 | 12 | 512 | 8 | 2,048 | **57,898,112（57.898M）** | 77,234,240 |
| XL 扩展大型 | 多说话人/多语言容量实验，16–24GB 优先 | 16 | 768 | 12 | 3,072 | **172,417,664（172.418M）** | 191,753,792 |
| **B1 十亿级** | 大数据教师、蒸馏和容量研究 | 24 | 1,536 | 24 | 6,144 | **1,028,035,712（1.028B）** | 1,047,371,840 |
| **B2 二十亿级** | 更大教师/规模化研究，不作消费级默认 | 28 | 2,048 | 32 | 8,192 | **2,128,957,568（2.129B）** | 2,148,293,696 |

完整声学列统一加上第 5.3 节的 **19,336,128** 个现有前端和辅助解码器参数，仍不含独立 variance、声码器和额外适配器。M→L 或 B1→B2 都是更换架构，需要匹配训练权重；不是运行时调大一个配置键即可得到能力提升。

#### 权重和优化器状态下限

| 档位 | 骨干 FP16/BF16 权重（GiB） | 骨干选择性 W4A16 权重（GiB） | 完整声学 Adam 16 bytes/参数状态（GiB） | 指定层 rank-8 LoRA 参数 |
| --- | ---: | ---: | ---: | ---: |
| S | 0.0140 | 0.0074 | 0.400 | 196,608 |
| M | 0.0410 | 0.0212 | 0.616 | 393,216 |
| L | 0.1078 | 0.0551 | 1.151 | 786,432 |
| XL | 0.3212 | 0.1629 | 2.857 | 1,572,864 |
| B1 | 1.9149 | 0.9656 | **15.607** | 4,718,592 |
| B2 | 3.9655 | 1.9967 | **32.012** | 7,340,032 |

选择性 W4A16 列按每块 QKV、attention output、MLP 共 $12LD^2$ 个权重用 0.5 bytes，其余骨干参数用 2 bytes 计算。它**不是整个模型都按 4-bit 存储**，也不含 scale/zero-point、打包、反量化缓冲或浮点副本。完整声学的浮点非骨干权重还需约 0.0360 GiB。优化器状态列不含激活、临时缓冲、CUDA context 和安全余量，更不是显存实测。

#### 消费级硬件与起步配置建议

所有建议均以单卡、micro-batch 1、SDPA、训练逐块重计算、不并行驻留教师/声码器为前提；帧数是**初始实验长度上限**，必须落实第 6 节的数据切分/对齐策略。容量可装入不代表速度合理或质量更好。

| 档位 | 全参微调起步 | LoRA 起步 | 推理参考 | 不应作出的承诺 |
| --- | --- | --- | --- | --- |
| S | 8GB，768 帧，累积 8 | 8GB，768 帧，rank 8 | 8GB，1,024 帧起测 | 更小骨干不自动补偿条件/辅助解码器成本，也不保证同音质 |
| M | 8GB，768 帧，累积 8 | 8GB，768 帧，rank 8 | 8GB，1,024 帧起测 | 本计划默认；8GB 仍需真实峰值验收 |
| L | 优先 12–16GB，768 帧，累积 8；8GB 可从 512 帧试验 | 8–12GB，512–768 帧，rank 8/16 | 8GB 可从 768 帧起测 | 不把“参数状态只有 1.15 GiB”当成 8GB 全流程保证 |
| XL | 优先 24GB，512–768 帧，累积 8；16GB 需测 | 优先 12–16GB，512 帧，rank 8/16 | 8–12GB，512 帧起测，按需量化 | 8GB 推理可研究，不列为 8GB 全参微调默认 |
| B1 | 多卡分片或 48GB 级资源优先；24GB 需专门短序列/状态优化实验 | 优先 16–24GB，256–512 帧，rank 8/16；8GB 仅选择性低比特底座实验 | 8GB 可研究 W4A16、256–512 帧；优先 12–16GB 浮点部署 | **16 bytes/参数全参状态本身已超 8GB**；8GB 低比特 LoRA/推理均未验证 |
| B2 | 多卡分片或 80GB 级资源优先；48GB 短序列需专门验证 | 优先 24GB，256 帧起步；16GB 量化底座需测 | 优先 16–24GB；8GB 低比特短句仅研究，不承诺 | 全参状态已约 32 GiB；不存在“通用 8GB 全参微调” |

LoRA 表中的 8/16 是候选 rank；资源表只计算 rank 8，rank 16 的适配器参数和对应训练状态翻倍。全参微调沿用 `lr=1e-4` 作为 S/M 起点；L/XL/B 级优先从 `5e-5` 起试，LoRA 从 `1e-4` 起试，仍须按数据量和验证曲线调节。新增全参/量化训练模式不得沿用不匹配的优化器状态。

B1/B2 的浮点 LoRA 只有在**冻结底座确实以 BF16/FP16 存储**时才可使用表中半精度权重预算。若仍用 FP32 底座，B1/B2 骨干分别约 3.83/7.93 GiB；`16-mixed` 本身不会自动改变这个事实。底座加载、LoRA 注入、量化转换最好在 CPU 分片处理再上卡，避免加载时同时保留完整浮点与量化 GPU 副本。

8GB 上研究 B 级 LoRA 应采用经过验证的选择性低比特冻结底座、256 帧短序列、逐块重计算和梯度累积，必要时显式 CPU offload；必须记录 offload 的主存需求和传输代价。不能为了通过测试把共享 GPU 内存换页隐藏成“8GB 内运行”，也不能宣称现有 `LoRALinear` 已支持低比特底座。

#### B 级的算量、数据与导出代价

按第 6.2 节公式，`B=1,T=768` 时主体 attention+MLP 单次计算约为 S **10.87**、M **28.99**、L **72.48**、XL **202.94**、B1 **1,130.65**、B2 **2,299.95 GFLOPs**。B1/B2 在 20 NFE 下约 **22.61/46.00 TFLOPs 每乐句**，尚不含前端和声码器。即使权重装得下，也不能宣称消费卡能实时运行；降低 NFE 仍需第 8 节的质量验证。

- B 级定位首先是多说话人/多语言、大规模授权数据的教师与蒸馏研究；小规模单人数据更容易过拟合，不应以参数更多代替数据质量和对照实验。不在这里编造“达到 B 级就必然提升音质”的缩放规律。
- 保留 `H=256` 是兼容性选择，可能成为大模型条件表达瓶颈；若实验表明要扩展 FS2 或改成 cross-attention，应作为新架构版本重新计数和验收，而不是继续沿用上表。
- B1 的 FP32、B2 的 FP16/FP32 单骨干权重已超过 2 GiB；ONNX 应评估 external data、图优化器的 2GB 限制、宿主加载和分片资源路径。现有“一次加载/简化/合并完整图”的导出流程不能未经验证就用于 B 级。[S8]
- B1/B2 的主干宽度均可被 256 整除，为 K-quant 主体矩阵布局提供更合适条件，但输入/输出等小矩阵与实际 kernel 仍要分别检查；不等于自动获得 GGUF 执行支持。
- 优先路径是 **B 级教师离线生成目标 → M/L 学生少步蒸馏 → 消费卡部署**；部署 B 级本身只有在质量收益足以抵偿延迟与维护成本时才值得默认化。

## 6. 8GB 显存：预算、默认训练方案与长序列处理

### 6.1 单位和不可忽略的开销

本节 `MiB=2^20 bytes`，`GiB=2^30 bytes`；参数量 M 按十进制百万。8GB 型号的可用专用显存以实际设备报告为准，Windows 桌面和其他进程也会占用。目标是**单模型工作峰值不超过约 6.5–7 GiB，至少保留约 1 GiB 余量**，而不是把显存填满。

$$
M_{\mathrm{peak}}=M_{\mathrm{weights}}+M_{\mathrm{grad/optim}}+M_{\mathrm{saved\ activations}}+
M_{\mathrm{workspace}}+M_{\mathrm{CUDA/runtime}}+M_{\mathrm{other\ models}}
$$

FP32 主参数、FP32 梯度、Adam 两个 FP32 状态合计约 **16 bytes/可训练参数**；这不是所有框架/优化器都完全相同的定律。foreach 临时缓冲、混合精度转换、额外 master copy、EMA 和 CUDA allocator 另算。

- 完整声学 41,355,584 参数的上述状态约 **0.616 GiB**。
- 完整声学 FP16 权重约 **78.88 MiB**；FP32 约 **157.76 MiB**。
- 单 DiT FP32 / FP16 / 理想全 INT8 / 理想全 4-bit 权重分别约 **84.00 / 42.00 / 21.00 / 10.50 MiB**。后两项仅为数学存储下限，不是实际量化成品大小。
- 普通 LoRA 若底座仍以 FP32 存储，底座仍占约 157.76 MiB；`16-mixed` 不自动把参数存储减半。

### 6.2 attention 为什么是关键

44.1kHz / hop 512 对应约 86.13 帧/秒。普通显式 attention 一个 score 张量的大小为 $B h T^2 s$，$s$ 是元素字节数。以下仅是 **batch 1、6 heads、FP16、一个张量**，尚未包含 Softmax、梯度、其他层和前端：

| 帧数 | 时长 | 单个 score 张量 |
| ---: | ---: | ---: |
| 768 | 8.92 秒 | 6.75 MiB |
| 1,024 | 11.89 秒 | 12.00 MiB |
| 2,048 | 23.78 秒 | 48.00 MiB |
| 6,000 | 69.66 秒 | 411.99 MiB |
| 10,000 | 116.10 秒 | 1,144.41 MiB |

FP32 对应翻倍。采用内存高效 SDPA 可以避免显式常驻完整 score 矩阵，但**不会消除全局 attention 的二次计算量**；mask、dtype、设备或图模式可能使实现回退到数学路径，必须检查实际选择。

重计算后，8 层入口残差的一个简化下限是 $8BTD\times2$，在 `B=1,T=768,D=384` 时仅 4.5 MiB；一个 `4D` MLP 激活约 2.25 MiB。它们只是张量算例，**不能当作总激活峰值**。

默认骨干 attention+MLP 主要 FLOPs，按一次乘加记 2 FLOPs：

$$
\mathrm{FLOPs}\approx L(24BTD^2+4BT^2D)
$$

768/1,024/2,048 帧单次分别约 28.99/41.88/109.52 GFLOPs；20 NFE 分别约 579.82/837.52/2,190.43 GFLOPs。不含前端、投影、调制、归一化和声码器；不能除以显卡标称峰值就声称实际 RTF。

### 6.3 推荐 8GB 训练/微调档

| 项目 | 全参微调默认 | LoRA 默认 |
| --- | --- | --- |
| 模型前提 | 已训练的同架构 DiT 基座 | 同架构 DiT 基座，严格匹配 |
| micro-batch | 1 | 1 |
| 单条训练片段 | 最多 768 帧，先选短完整乐句 | 最多 768 帧起步 |
| 梯度累积 | 8，即约 8 条样本/更新，按实际有效帧记录 | 8 |
| 混合精度 | BF16 支持且验证通过时用 `bf16-mixed`；否则 `16-mixed` + scaler | 同左 |
| 优化器 | AdamW，lr `1e-4`，betas `(0.9,0.98)`，weight decay `0.01` | AdamW，lr `1e-4`，betas `(0.9,0.98)`，weight decay `0` |
| 适配器 | 关闭 | rank 8，alpha 16，`train_bias=false` |
| 重计算 | DiT 每块开启；必要时辅助解码器也支持重计算 | 同左，冻结底座不等于无需保存激活 |
| attention | SDPA，非因果，有效帧 mask | 同左 |
| 梯度裁剪 | global norm 1.0 | global norm 1.0 |
| 学习率策略 | 前 500 次 optimizer update 线性 warmup，再 cosine；按验证早停 | 同左 |
| 验证档 | batch 1、同样长度上限；试听与训练错峰执行 | 同左 |

这里的 lr/调度是初始工程建议，不是经过本数据集网格搜索的最优值。对极小数据集可先降至 `5e-5`；从头训练 DiT 需要另定数据规模和更长训练预算，不应把微调设置包装成低成本预训练保证。

已有键可用 `max_batch_size=1`、`max_batch_frames=768`、`accumulate_grad_batches=8` 表达批次预算；**sampler 的帧预算不是单样本自动裁剪器**。必须在数据准备/样本读取阶段落实最大长度，新增长度策略并测试。优先按已有乐句或词/音符边界切分，同步重映射 mel2ph、note/word 对齐及所有帧曲线；不能随机裁 mel 而保留原音素/时长标签。

#### RTX 4090 24GB、约 10 小时数据的推荐起点

以下是未在 4090 上实测的起始建议，不称为经过网格搜索的“最优值”；模型层数、宽度、头数和参数量均保持不变，单样本继续限制 768 帧：

| 模型 | micro-batch / `max_batch_size` | `max_batch_frames` | `accumulate_grad_batches` | `val_check_interval` |
| --- | ---: | ---: | ---: | ---: |
| 声学 | 8 | 6144 | 1 | 500 trainer batch |
| variance | 16 | 12288 | 1 | 500 trainer batch |

两者验证 batch 均为 1、验证帧预算 768，精度从 `bf16-mixed` 起步。`max_batch_frames` 是变长 batch 的总帧预算，因此实际 batch size 可能小于上限。对应级联与用户模板位于 [configs/dit](configs/dit/README.md)：`config_acoustic_4090_10h.yaml` 和 `config_variance_4090_10h.yaml`。若完整训练/验证路径超出专用显存，优先同比降低 batch 上限与总帧预算，不改模型参数量，也不放宽单样本长度。

本次采用以下保守的**容量规划预留，不是运行时测量**：

| 预算项（B=1、T≤768） | 全参微调预留 |
| --- | ---: |
| 参数/梯度/Adam 状态 | 约 0.62 GiB |
| 前端、辅助解码器、DiT 保存激活及重计算峰值 | 0.5–2.0 GiB（待测假设） |
| 算子/优化器临时缓冲与 allocator 余量 | 0.5–1.5 GiB（待测假设） |
| CUDA context、库及其他运行时 | 0.5–1.0 GiB（待测假设） |
| 安全余量 | 1.0 GiB |
| **规划合计** | **约 3.1–6.1 GiB**，不含同时驻留的独立模型或声码器峰值 |

结论是**具备在 8GB 上实现的合理空间，而非已经证明所有 8GB 卡都能跑**。LoRA 主要减少优化器/梯度状态；激活仍是重要成本，不能保证比全参微调低一个数量级。

### 6.4 必须控制的验证与推理峰值

- [现有验证入口](basics/base_task.py#L279-L290) 显式关闭 autocast；即使训练为混合精度，验证仍可能成为峰值。新档位需审计该路径：可采用短验证样本，或经数值验证后单独启用验证混合精度；不能只改 Trainer precision。
- 8GB 档不在同一时刻训练 acoustic、variance 和声码器。声码器冻结，音频试听延后/独立执行；保留用户启动完整验证的能力，不删掉功能。
- DiT 默认 warmup 只做 128/512/768/1,024 帧，不沿用 10,000 帧。原生、ONNX、量化各自选择经过验证的路径。
- 推理 `B=1`，默认按乐句处理；中等长度采用全局 SDPA。超长请求先检查资源，不能无提示触发共享 GPU 内存换页。
- 需要保持全局语义时，可开发**逐层 query 分块、关注全部 K/V**的数学 attention 低内存路径，或使用已验证的内存高效 kernel；前者降低 score 峰值但不降低二次总算量，不等同于局部注意力。
- 按时间分段独立生成再 overlap/crossfade 是有损近似，会改变全局 attention。若提供，必须显式标记近似模式、同步条件/噪声/位置并做接缝试听；不能称为与整句推理数值等价。
- 若 OOM，先从 768 帧降至 512、确认重计算/SDPA 是否生效、排除并发声码器；不自动删控制条件或偷偷改模型宽度。

验收时记录设备型号、驱动、PyTorch/CUDA、dtype、采样器/NFE、长度与 batch、`max_memory_allocated`、`max_memory_reserved` 和进程专用显存。显存/速度必须分别覆盖训练、优化器更新、验证、加载、预热、声码器及连续请求；不只记录一次 backbone forward。

## 7. 量化友好设计与实际收益

### 7.1 默认量化范围

第一阶段只量化每块 QKV、attention output 和 MLP 的**权重**；保留 LayerNorm、Softmax、RoPE、残差、时间 MLP、adaLN 调制、输入/输出投影和求解器累加的合适浮点精度。Softmax/归一化必要时 FP32 累积。不同后端的 BF16 支持不等同，ONNX 优先提供经过验证的 FP32/FP16 包。

默认 DiT 上述主体矩阵共 $L\times12D^2=14,155,776$ 个权重，占骨干参数约 **64.29%**。adaLN 参数多但每次仅对时间向量计算，不应为了追求量化覆盖率优先牺牲它。

| 存储方案 | 单 DiT 估算，不含 scale/zero-point/布局开销 |
| --- | ---: |
| 全 FP16 | 42.00 MiB |
| 主体矩阵 INT8，其余 FP16 | 28.50 MiB |
| 主体矩阵 INT4，其余 FP16 | 21.75 MiB |

其余声学前端和辅助解码器仍 FP16 时，完整声学约为 **65.38 MiB（选择性 INT8）** 或 **58.63 MiB（选择性 INT4）**，而不是把整模按 4-bit 算成约 20 MiB。scale、padding、宿主保留浮点副本、运行时重新打包还会增加内存。

**对 22M 骨干，4-bit 节省的只是几十 MiB 级权重，而不是数 GiB。** 8GB 友好主要依靠合理规模、长度管理和注意力实现；量化的主要额外价值可能在 CPU/边缘部署、内存带宽和分发体积。GPU batch 1 的反量化开销可能抵消收益，必须和 FP16 实测比较。

### 7.2 分阶段路线

1. **浮点基线**：先校验 FP32 与混合精度的单次预测和完整采样。固定噪声和条件，排除随机差异。
2. **W8A16 weight-only**：主体矩阵 per-output-channel 或后端支持的分组量化；这是逻辑精度方案，具体内核是否直接支持要单独验证。
3. **ONNX CPU INT8**：动态激活量化或静态 QDQ 是不同路径，不能与 W8A16 混称。静态量化需完整声学校准；动态方案需评估每步额外开销。
4. **W4A16**：主体矩阵采用 group size 128 作为 ONNX 实验起点，验证小矩阵和反量化开销。若某层误差大，回退该层 FP16/INT8。
5. **W8A8 / QAT**：只有 PTQ 无法达标或需要实际低比特 GEMM 加速时才投入；SmoothQuant 是参考，不自动解决时间调制和跨步误差。

校准集建议从 200–500 条经授权、未参与最终评价的代表性乐句起步，覆盖说话人/语言、长度、低高音、清浊音、气声/力度/张力、gender/speed、artifact 模式、浅扩散深度。按噪声时间分层，并收集实际 sampler 轨迹上的中间状态，不能只喂干净 mel。若新增 CFG，还须覆盖条件与无条件分支。

量化评估既比较单层/单步误差，也比较累积后的 mel、f0/variance 曲线和音频。以固定浮点基线消融，允许建立层级精度白名单；若需要每个时间区间不同量化参数，应将其作为显式版本和部署复杂度项，而不是暗中增加多套模型。

### 7.3 LoRA 与 QLoRA 的区别

- 现有 LoRA 是浮点线性层上的低秩更新，**不是 QLoRA**。
- 第一版优先普通 LoRA：当前骨干不大，FP32 底座都只有约 84 MiB；为省几十分 MiB 引入额外依赖通常不是首要收益。
- QLoRA 作为可选研究：冻结 NF4 底座，在 BF16/FP16 计算中训练浮点 LoRA；激活和前后向并非全 4-bit。
- NF4、ONNX INT4、GGUF Q4 编码不能直接互换；Windows/显卡/库版本支持需另验，不能宣称安装某个库后所有环境都支持。
- 部署推荐：从浮点基座载入 → 载入匹配 LoRA → 合并成普通 Linear → 验证合并前后等价 → 重新校准/量化 → 导出。不要直接在低比特整数权重上相加浮点 LoRA，也不要把每次合并结果再当原始基座。
- 新说话人/新音素若不在原映射中，单纯骨干 LoRA 无法凭空增加正确的 embedding；需要明确的嵌入扩展与 `modules_to_save` 类能力及新参数统计，不能将“已有音色适配”混同于“无条件新增身份”。

## 8. 单步推理：数学上可行，但要独立训练与定义

### 8.1 本仓库 Reflow 的时间方向

本仓库实际训练为：

$$
x_t=(1-t)x_0+t x_1,\quad x_0\sim\mathcal N(0,I),\quad
v_\theta(x_t,t,c)\approx x_1-x_0
$$

从 $t_s$ 到 1 的一次 Euler 评估是：

$$
\hat x_1=x_{t_s}+(1-t_s)v_\theta(x_{t_s},t_s,c)
$$

代码层面将 Euler 的 `sampling_steps=1` 确实可以执行这一步；但训练插值是直线，不代表学到的条件速度场的实际积分轨迹处处为直线。条件不完备、多解性和估计误差都会使一步离散化误差显著。

浅扩散时使用 $x_{t_s}=(1-t_s)z+t_s x_{aux}$，通常更接近目标；但训练中的 $x_1$ 是真实 mel，推理中的 $x_{aux}$ 是预测 mel，存在分布差异。可研究**辅助解码器 + 一次 DiT 修正**，但不能据此保证清晰度和细节。

### 8.2 需要区分的“单步”

| 说法 | 实际含义 |
| --- | --- |
| Euler 1 步，无 CFG | 一次骨干评估，质量待测 |
| RK2 / RK4 / 当前 RK5 各 1 步 | 分别需要 2 / 4 / 6 次骨干评估，不是 1 NFE |
| 辅助解码器 + 1 次 DiT | 是 1 次 DiT，但不是整个歌声系统只执行一个网络 |
| 只用辅助解码器 | 0 次 DiT；不是证明扩散已能高质量单步生成 |
| 声学 1 NFE | 不代表 pitch、variance、声码器也被消除 |
| 带 CFG 的“1 步” | 还需条件/无条件预测；即使合 batch，一次调用的算量也更大 |

DiT、SiT 和 F5-TTS 均不能作为“替换骨干即可单步”的依据。原始 DiT/SiT 的主要结果仍使用多次网络评估；F5-TTS 的非自回归与 Sway Sampling 也不等于一步生成。[S1–S3]

### 8.3 推荐研究顺序

1. 先训练质量合格的 **20-NFE DiT 教师**，锁定特征/采样/声码器基线；公平比较时教师可用更多 NFE，但要单列计算预算。
2. 不改变训练先比较 20/16/8/4/2/1 Euler NFE，找出可接受的少步区间；这是诊断，不等同蒸馏。
3. 优先研究浅扩散的一步修正学生：用实际 `x_aux`、相同初始噪声、全部条件和深度生成教师目标。缓存教师结果，避免教师+学生同时占 8GB GPU；缓存需授权与足够磁盘容量。
4. 若希望真正从噪声一步生成，再比较 consistency distillation 与 rectified-flow 的 reflow/蒸馏；学生需专门的一致性/端点目标或轨迹直化训练，不能照搬图像 LPIPS 当声学指标。[S4–S5]
5. 可从多步逐级降低到 8→4→2→1，并联合 mel、频谱、音高、条件保真和人工盲听评价；是否使用波形感知损失取决于冻结声码器的额外训练成本。
6. 单步质量达标后再量化单步学生，避免同时更换 NFE 与精度导致误差来源不可定位。

学生包要标注 `distilled`、教师哈希、训练起点/深度范围、预期 NFE 和支持的采样模式。一步 endpoint 学生未必支持旧求解器或任意深度；保留多步教师包作为完整功能回退。若只验证固定 `T_start=0.4`，就不能宣称变量深度仍然全部兼容。

## 9. ONNX 化：优先兼容既有宿主

### 9.1 可行性与分层产物

拟议 1D DiT 可以用 Linear/MatMul、Add/Mul、Reshape/Transpose、ReduceMean、Softmax、Sin/Cos、GELU 的基础表达实现，**理论上可 ONNX 化**。难点主要是动态长度、mask、trace 分支、采样循环及目标 EP 内核，而不是“Transformer 不能导出”。

1. **兼容产物**：沿用现有外部输入和 `steps/depth` 语义，内部 attention 导出为普通算子；先保留 opset 15 路径并显式分解 LayerNorm/GELU/RoPE。在旧 `torch==1.13.1` 导出环境独立验证，不能只在新环境导出成功就声称旧宿主兼容。
2. **现代浮点/INT8 产物**：独立导出环境和版本清单，可评估 opset 17/18；必须匹配实际宿主 ORT 和 EP，不全局替换旧模型包。
3. **现代 INT4 产物**：按 ORT 官方工具要求处理 opset 21、`MatMulNBits` 或 QDQ；含自定义域节点时不能声称任意 ONNX 引擎通用。[S8]

当前 ONNX 包是 batch 1，优先保留这一约束，只做长度动态；动态 batch 作为另一个显式能力。有效长度由外层 durations/对齐数据产生并传到内部 mask，无需强迫旧宿主增加新输入。独立骨干调试图可暴露 `valid_mask`。

### 9.2 采样器放在哪里

- **完整图**：保留现有 FS2/aux + sampler 的图组合，既有宿主无需实现新 solver。要验证 `Loop` 内部 attention、动态 `steps` 和 `depth`。
- **骨干一步图 + 宿主求解器**：另导出一次速度/噪声评估，便于量化、校准、固定外部噪声、计时和 GGML 对拍。宿主负责反复调用，并用 I/O Binding 保持张量在设备上。
- 第二种不能在未修改宿主的情况下替代第一种；第一版必须保留完整兼容包。
- 对含 `Loop` 的图，确认量化器是否递归处理子图。避免“工具执行成功，但循环体没有真正量化”的假阳性。

### 9.3 导出与量化顺序

1. 校验完整浮点 checkpoint 和配置；处理 LoRA 合并；关闭训练重计算、训练 dropout、临时优化包装。
2. 选择专用数学 attention 实现，不在 trace 内根据输入长度做 Python 分支，不将动态 `T` 转成固定 Python 整数；使用张量 shape 的 reshape 和位置生成。
3. 导出 FP32 基线，运行 ONNX checker/shape inference；对简化前后图进行数值对拍。
4. 验证不同长度、padding、F/M 组合和浅扩散边界；同 seed 不足以保证跨引擎相同噪声，数值对拍需显式注入同一份噪声。
5. 按目标后端转换混合精度或量化。INT4 的固定权重 MatMul 可处理，但 attention 的 `QK^T`、`AV` 不是常量权重 MatMul，不能套用同一 weight-only 规则。
6. 运行实际宿主测试，记录 EP 分配和 CPU fallback。CUDA、TensorRT、CPU、DirectML 等能力不同；成功加载不代表低比特 kernel 真正执行，更不代表提速。

保留旧 DDPM 采样和 Reflow Euler 导出能力；不要为 DiT 强迫所有旧模型采用一个新 opset。现代与 legacy 依赖分别锁版本，最终交付同时携带运行时最低要求、测试环境和不支持的功能清单。

## 10. GGUF 化：可存储，但需要专用 GGML 执行适配

### 10.1 GGUF 与 GGML 的职责

GGUF 是权重/张量和元数据的文件格式；GGML 是构建/执行张量计算图的技术基础。官方 GGUF 规范中通用 computation graph 仍属于讨论中的扩展；现有执行器必须认识模型架构。[S9]

因此：**不能只把 DiT 权重转换成 GGUF，就期望 llama.cpp 或任意 GGUF 应用直接唱歌。** 新架构需要张量映射、加载器、图构建、算子与后端适配；llama.cpp 官方新增架构说明也明确区分这些步骤。[S10]

### 10.2 推荐最小路线

1. 先做骨干级 GGUF：PyTorch/ONNX 前端产生 `cond` 与辅助 mel，GGML 执行 DiT，宿主执行 Reflow/DDPM 求解器；声码器维持原路径。明确这是混合部署，不是整个系统一个文件即可运行。
2. 首先导出 F32/F16，核对矩阵转置、张量维度、QKV 排列、RoPE 旋转约定、LayerNorm epsilon、GELU 近似、时间方向和输出恢复。
3. 再做选择性 `Q8_0`、`Q4_0`，敏感层保留浮点；记录实际块大小和开销，不用“Q4”作为没有定义的统一精度名。
4. 需要完整原生部署时，再适配 FS2、长度调节器、辅助解码器、variance、字典/映射和声码器，或明确保留这些依赖的外部模型包。

建议私有架构标识使用符合规范字符集的 `dsrxdit`，并以 `dsrx.*` 命名空间记录：版本、层数/宽度/头数/MLP、mel 规格、条件维度、时间 embedding/scale、RoPE、生成目标、浅扩散范围、spec min/max、F/M、tensor layout、LoRA 合并信息、量化方式、前端/声码器关联哈希。不是假装 `llama` 来复用错误的加载器。

### 10.3 默认宽度与 GGUF 块约束

宽度 384 可被 32 和 128 整除，适合本方案主体 Linear 的 `Q4_0` 类 32 元素块及 group-128 实验；**384 不能被 256 整除**，不能默认所有权重都能无条件用 `Q4_K` 等 256 元素块布局。

第一版优先使用实际支持的 Q8_0/Q4_0；不支持的矩阵保留浮点。若需要 padding、换布局或其他 K-quant，必须验证相应转换器/内核与计算正确性，而不是只修改文件标签。不得为一个量化格式偷偷改变已训练模型宽度。

GGML 推理也不是自回归语言模型：每个去噪时间都重新计算与当前 `x_t` 相关的 QKV，不复用上一时间步的 K/V 缓存。可以缓存与当前噪声状态无关的条件投影，但需单独核验内存收益和结果。

CPU 可用性、GPU offload、Windows CUDA/Vulkan 支持与速度均需在锁定提交/构建配置后验证。先做到浮点数学对拍，再量化，最后评价是否值得维护这一条独立执行链。

## 11. 现有功能保留矩阵

本矩阵是**实施验收清单**，不是声称当前新 DiT 已通过。通过旧模型回归与新 DiT 功能测试两层验证；旧 ONNX 本来受限的能力按原有范围保留，不虚构扩展。

| 功能 | 保留方式 | 必须覆盖的验证 |
| --- | --- | --- |
| 旧三种骨干与历史配置 | 注册增量、原兼容回退不变 | 各骨干旧 checkpoint 加载及固定样例回归 |
| DDPM / Reflow | 仅替换预测网络，目标与 core 保留 | 同 shape、时间输入、各自 loss 与合法 sampler |
| 浅扩散、aux-only、联合/分开训练 | 保留辅助解码器及训练开关 | `train_aux_decoder`、`train_diffusion`、`aux_decoder_grad`、`val_gt_start`、深度边界 |
| 音素/时长/音符对齐 | 保留现有 FS2、调节器与字典 | mel2ph、ph2word、mel2note 和长度一致性 |
| 时长预测 | 原 duration predictor 不替换 | 仅时长、仅 pitch、仅 variance 及组合开关 |
| pitch / melody / glide / expr | 保留原条件生成与裁剪 | 连音、滑音、长音、表达参数及 melody encoder 开关 |
| energy / breathiness / voicing / tension | 原参数适配器与顺序 | 1–4 曲线、repeat bins、归一化、clamp、retake |
| 多说话人、动态混声、语言 | 保留嵌入、映射和混合权重 | 原生与导出冻结/外露说话人，多语言及音素扩展 |
| gender/key shift、speed、artifact | 沿用声学条件链 | 值域边界；artifact 导出沿用现有冻结 clean 语义 |
| 数据增强和预处理 | 不改原定义，只增加安全长度策略 | 移调/时间伸缩后同步对齐，样本标签不串位 |
| 全参微调、冻结、LoRA | 原路径加严格校验与显式能力 | resume、基座加载、适配器保存/合并、匹配拒绝 |
| 推理随机种子、CLI、批量后端 | 用户协议不变，换模型包 | 重复请求、加载失败回退、请求隔离、精确输出长度 |
| 原生精度优化与 TorchScript | 按 DiT 能力选择路径 | 普通/优化结果对拍、动态长度、mask、可关闭优化 |
| NSF-HiFiGAN / RefineGAN | 声码器独立，原优化保留 | mel base/f0/采样率、长句分块、相位与长度 |
| ONNX 导出、字典与附属资源 | 保留 legacy 包结构 | 宿主加载、动态步数/深度、冻结选项与资源完整性 |
| 推理专用 checkpoint | 保留模型状态及完整配置 | [现有精简导出流程](scripts/export_inference_ckpt.py#L1) 与普通权重一致性 |
| 超长输入 | 显式资源策略，不静默截断 | 短长交替、内存回收；近似分块必须明确标识 |

## 12. 实施工作包与完成条件

所有工作包下的“完成”均指未来实现阶段。不能因为本计划写完而标记代码功能完成。

### P0：冻结兼容基线与接口契约（已完成）

- [x] 在 [DiT P0 基线](configs/dit/p0_baseline.yaml) 记录当前分支/提交、旧骨干、典型声学/variance 输入输出、checkpoint 配置快照约定、ONNX opset 15 与 PyTorch 1.13.1 宿主基线。
- [x] 冻结 `valid_mask` 来源与语义、checkpoint 架构字段、架构/LoRA 不匹配拒绝规则，以及旧三参数骨干不受影响的适配边界。
- [x] 创建五项最小功能矩阵，并提供只解析配置、不实例化未实现后端的 [P0 校验脚本](scripts/validate_dit_configs.py)。
- [x] 将 DiT 声学/variance 级联配置和用户模板集中到 [configs/dit](configs/dit/README.md)；通过独立 reset 层清除父配置中的卷积专用参数，并显式关闭旧声学配置继承的 LoRA。
- **完成结果**：P0 的仓库基线、配置链和接口契约已可复核；当前工作区没有 acoustic/variance checkpoint，因此只冻结旧配置与接口基线，不虚构旧权重数值回归。DiT 注册、mask 传递、训练/推理和导出仍属于 P1–P3，配置文件在 P1 前不能用于模型实例化。

### P1：浮点 DiT、训练与 8GB 档

- [x] 新增独立 DiT 实现，在 [骨干工厂](modules/backbones/__init__.py#L7-L20) 注册；按第 4 节结构提供显式 Linear。
- [x] 在 [顶层模型](modules/toplevel.py#L32)、[Reflow core](modules/core/reflow.py#L13)、[DDPM core](modules/core/ddpm.py#L1) 传递有效帧信息；旧骨干保持原三参数调用。
- [x] 加入逐块重计算、按有效元素归一化的 loss mask、768 帧数据集硬限制和严格配置校验；声学/pitch/variance 模板分别选择自己的骨干。
- [x] 验证 [训练入口](basics/base_task.py#L59) 的基座加载/LoRA 注入/冻结顺序；DiT LoRA 零命中或基座缺失会中止，并报告命中层、可训练参数数和基座哈希。
- **完成条件**：shape/梯度/公式计数吻合；B=1/2、T=1/奇数/不同 padding、F=1/多特征无 NaN；能在目标 8GB 设备进行稳定微调，而不只 forward 一次。
- **完成结果（2026-09-19）**：P1 行为与配置脚本通过；完整 acoustic、pitch、多 variance 训练前向及 legacy acoustic 回归通过。RTX 5060 Laptop 8151 MiB 上以 BF16、768 帧、micro-batch 1、累积 8 完成 20 次 optimizer update，loss 全部有限，PyTorch 峰值 allocated/reserved 为 835.8/890.0 MiB。该结果仅证明合成数据下的工程稳定性，不声称真实数据收敛、音质或端到端推理性能。

### P2：原生推理与功能回归（已完成）

- [x] 适配 [原生优化器](inference/optimization.py#L200)、[声学推理](inference/ds_acoustic.py#L1)、[variance 推理](inference/ds_variance.py#L1) 和 [批量后端](inference/batch_backend.py#L1)。
- [x] 对 LoRA/特殊 Linear 保护、动态长度、warmup、模型切换和长序列实施安全策略。
- **完成条件**：完整功能矩阵通过；质量与速度有与旧模型对照的实际报告；无共享显存换页假装 8GB 内运行。
- **完成结果（2026-09-19）**：mask-aware DiT 保持 eager，legacy 三参骨干保留 TorchScript；DDPM/Reflow、acoustic B=2 padding、pitch、多 variance、严格推理 LoRA、模型切换失败回滚和 legacy LYNXNet2 顶层推理通过。RTX 5060 Laptop 上 768 帧 20 步 Reflow 为 0.385 秒、峰值 allocated 67.8 MiB；2048 帧骨干边界前向为 0.025 秒、峰值 allocated 90.0 MiB，2049 帧在预处理入口拒绝。仓库缺少训练 checkpoint，故音质及旧模型同权重数值/速度对照不可执行；以上仅为随机权重合成功能与资源 smoke，不声称音质。

### P3：ONNX 浮点兼容链（已完成）

- [x] 适配 [声学导出器](deployment/exporters/acoustic_exporter.py#L201)、[variance 导出器](deployment/exporters/variance_exporter.py#L1) 与已有 sampler wrapper；DiT 走部署专用基础算子 attention，legacy 骨干保持原三参 trace。
- [x] 保持 batch 1、动态帧轴、opset 15 和现有宿主输入协议；部署 adapter 从 condition 长度构造全有效 mask，不给旧宿主增加输入。
- [x] DiT 导出 LoRA 按“严格加载底座 → 注入 → 严格加载全部 adapter → 可选 merge”执行，legacy LoRA 路径不变。
- **完成条件**：checker 成功只是第一项；还需实际宿主、不同长度/深度/步数数值对拍和功能一致性。
- **完成结果（2026-09-19）**：PyTorch 2.11 的 legacy TorchScript exporter 生成 opset 15 图，ONNX 1.16.2 checker 通过；ONNX Runtime 1.23.0 CPU 对 T=1/7/16 与 PyTorch 数值对拍通过，legacy 三参骨干及浅 Reflow wrapper 回归通过。修复了 legacy exporter 对负轴 `transpose`/`softmax` 的错误 lowering。当前机器没有 PyTorch 1.13 环境，也没有已训练 acoustic/variance checkpoint，因此未声称锁定环境、最终合并模型包、实际 DiffSinger 宿主或音质已经实测；这些属于拿到训练权重后的发布验收。

### P4：量化与 LoRA 交付

- **前提：已有训练完成的浮点 DiT checkpoint，以及代表性校准集和独立评价集。**
- 先验证普通 LoRA 合并，再建立校准集与选择性 INT8；随后尝试 INT4 和必要的 QAT。
- 比较实际文件大小、驻留显存、端到端延迟和音质；记录哪些层以及哪些 EP 真正在低比特执行。
- **完成条件**：可解释的精度退化与可复现收益；如不如 FP16，保留实验能力但不设为默认。

### P5：少步/单步学生

- **前提：已有训练完成且质量达标的多步教师模型。**
- 多步教师基线稳定后，按第 8 节开展；在 8GB 设备上优先离线教师缓存。
- **完成条件**：在相同条件覆盖和盲听方案下达标；明确 NFE、深度范围、采样限制；有多步回退包。

### P6：GGUF/GGML 实验部署

- **前提：已有训练完成的浮点 DiT checkpoint，可作为 PyTorch/ONNX/GGML 数值与量化对照。**
- 浮点骨干级 loader/graph 对拍，随后 Q8_0/Q4_0；决定是否继续做完整前端与声码器移植。
- **完成条件**：有真正执行该 DiT 的程序及构建/运行时清单，不只是生成了 GGUF 文件。

依赖关系：P0 → P1 → P2，P3 已在 P1 接口稳定后完成；P4 依赖已训练浮点模型与 LoRA/校准基线；P5 依赖已训练教师质量；P6 依赖已训练浮点 DiT，且不阻塞主要 PyTorch/ONNX 交付。不要一开始同时重写骨干、量化、单步和执行器。

## 13. 验证设计与发布门槛

### 13.1 数值与结构

- 同一乐句单独运行与 padding 后批量运行，有效区输出在容差内一致；特别检查 padding key 泄漏、全空输入和 query 输出清零。
- 测试时间广播、DDPM 整数时间/Reflow 浮点时间、负/非法形状拒绝、`F×M` 的展平/恢复顺序。
- 检查 LoRA 注入后的实际可训练数与估计一致，冻结权重无梯度，checkpoint 重计算后 LoRA 梯度非零；合并一次与未合并输出等价，重复合并不重复叠加。
- 在同噪声、同条件、同 solver 下比较 PyTorch eager/优化/ONNX/GGML；短输入 FP32 对拍可从 `atol=1e-5, rtol=1e-4` 开始，半精度按实际误差分布重新制定，不将此当跨所有模型的硬性公理。
- 所有量化器与图优化器都检查处理前后节点/张量覆盖率，防止工具“成功”但未处理目标骨干。

### 13.2 音质与控制保真

使用独立测试集，覆盖高低音、长音颤音、快速咬字、气声/清浊切换、休止、极端控制值、混声和多语言。建议至少 100 条代表性乐句用于初期回归，并保持训练/校准/最终评价集合分离。

- 定量：mel L1/频谱距离、f0 cents RMSE/相关性、清浊错误、时长误差、variance 曲线误差、输出长度和非有限值；内容/说话人相似度作为辅助，不取代歌声试听。
- 定性：盲听比较咬字、音高、节奏、音色、呼吸、金属伪影、长音稳定性与接缝。歌声任务不照搬图像 FID 或仅依赖语音识别分数。
- 候选非劣门槛可先约定“同一浮点模型量化后，关键客观误差增幅不超过 5%，盲听无系统性新增伪影”，再按指标尺度与统计置信区间收紧；**这是待确认的工程门槛，不是本次已达到的结果**。
- DiT vs 旧骨干、低比特 vs 浮点、单步 vs 多步三种比较分别进行，不能改变多个变量后将所有收益归功于 DiT。

### 13.3 资源与速度

8GB 验收覆盖至少一个目标桌面 GPU 和实际使用的 Windows 环境；若宣称笔记本支持，再覆盖对应设备。报告 warmup、首包延迟、稳态端到端 RTF、p50/p95、CPU/RAM/专用显存；分开记录声码器占比和纯骨干耗时。

NFE 与 solver steps 同时报告。对小模型不预设 INT4 一定快；以相同音质水平的端到端延迟做选择。所有测试在实现阶段有目的地执行；本次文档交付不启动训练、长时间 GPU benchmark 或自动化音频批量生成。

## 14. 一手研究依据与适用边界

检索/核对日期：2026-09-19。本次读取论文 HTML/摘要与官方文档，**没有下载或脚本解析 PDF**；下列 PDF 链接提供原文入口。参数和显存算例使用本地脚本完成。引用只支持对应论点，不把其他任务的测量数字外推为本仓库结果。

| 编号 | 一手来源 | 已读取范围与本文用途 |
| --- | --- | --- |
| S1 | Peebles & Xie, *Scalable Diffusion Models with Transformers*：[HTML v2](https://arxiv.org/html/2212.09748v2)、[PDF](https://arxiv.org/pdf/2212.09748) | HTML 的 DiT/adaLN-Zero 与评估相关章节；支持骨干设计，不支持“天然单步/天然可量化” |
| S2 | *SiT: Exploring Flow and Diffusion-based Generative Models with Scalable Interpolant Transformers*：[HTML v2](https://arxiv.org/html/2401.08740v2)、[PDF](https://arxiv.org/pdf/2401.08740) | HTML 的插值、目标与采样章节；骨干和生成目标是不同选择，时间方向不可混用 |
| S3 | *F5-TTS: A Fairytaler that Fakes Fluent and Faithful Speech with Flow Matching*：[HTML v3](https://arxiv.org/html/2410.06885v3)、[PDF](https://arxiv.org/pdf/2410.06885) | HTML 相关结构与采样实验；证明声学 DiT 有参考案例，不证明能直接替换歌声条件或获得一步质量 |
| S4 | *Consistency Models*：[HTML v2](https://arxiv.org/html/2303.01469v2)、[PDF](https://arxiv.org/pdf/2303.01469) | HTML 的一致性函数、边界条件和蒸馏/独立训练章节；一步能力需要专门目标 |
| S5 | *Flow Straight and Fast: Learning to Generate and Transfer Data with Rectified Flow*：[摘要](https://arxiv.org/abs/2209.03003)、[PDF](https://arxiv.org/pdf/2209.03003) | 仅核实摘要，HTML 正文读取未成功；支持把轨迹直化列为研究方向，不作为歌声一步质量保证 |
| S6 | *SmoothQuant: Accurate and Efficient Post-Training Quantization for Large Language Models*：[HTML v7](https://arxiv.org/html/2211.10438v7)、[PDF](https://arxiv.org/pdf/2211.10438) | HTML 方法与限制；等价缩放可改善 W8A8，量化仍有误差；LLM 收益不能直接移植 |
| S7 | *QLoRA: Efficient Finetuning of Quantized LLMs*：[HTML v1](https://arxiv.org/html/2305.14314v1)、[PDF](https://arxiv.org/pdf/2305.14314) | HTML 的 NF4 存储/BF16 计算和冻结基座 LoRA；不是全程 4-bit，也无声学质量保证 |
| S8 | [ONNX Runtime 官方量化文档](https://onnxruntime.ai/docs/performance/model-optimizations/quantization.html) | 静态/动态、QDQ、INT4 固定权重算子、opset 21、性能限制；具体 EP/版本仍需锁定验证 |
| S9 | [GGML 官方 GGUF 规范](https://github.com/ggml-org/ggml/blob/master/docs/gguf.md) | 文件结构、元数据、量化类型和 computation graph 扩展；格式不提供未知架构的自动执行 |
| S10 | [llama.cpp 官方新增模型架构指南](https://github.com/ggml-org/llama.cpp/blob/master/docs/development/HOWTO-add-model.md) | 转换、加载、架构定义、GGML 图及后端适配是独立工作 |

S8–S10 是滚动文档，实际实现时固定版本/提交再验证。论文图像/语音任务中的质量、速度和显存数值均不作为本计划的性能承诺。

## 15. 可复核计算方法

本次在所选 Conda Python 3.10.20、PyTorch `2.11.0+cu130` 环境执行了纯 CPU/`meta` 参数脚本，没有调用 GPU，也没有载入已有 checkpoint。实际做了两种交叉核验：

1. 第 5 节代数公式求和，与对应形状的 `torch.nn.Linear(..., device='meta')` 的 `sum(p.numel())` 完全一致。
2. 读取 [基础配置](configs/original/base.yaml#L1) 和 [原始声学配置](configs/original/acoustic.yaml#L1)，覆盖第 5.3 节明确的开关，实际实例化现有 FS2/ConvNeXt 并统计参数。

下面是骨干和 LoRA 数量的独立复算片段；只是规格计数，不是 DiT forward 实现：

```python
def count_dit(layers, width, channels, condition_dim=256, time_dim=256):
    d, c, h, e = width, channels, condition_dim, time_dim
    return (
        layers * (18 * d * d + 15 * d)
        + (c * d + d)                 # input
        + (h * d + d)                 # condition
        + (e * d + d + d * d + d)     # time MLP
        + (2 * d * d + 2 * d)         # final modulation
        + (d * c + c)                 # output
    )

assert count_dit(8, 384, 128) == 22_019_456
assert count_dit(6, 256, 128) == 7_495_808
assert count_dit(4, 256, 64) == 5_096_000
assert count_dit(4, 256, 48) == 5_087_792
assert count_dit(12, 512, 128) == 57_898_112
assert count_dit(16, 768, 128) == 172_417_664
assert count_dit(24, 1536, 128) == 1_028_035_712
assert count_dit(28, 2048, 128) == 2_128_957_568
assert 8 * 16 * 8 * 384 == 393_216   # layers * 16 * rank * width
```

这只证明结构规格下的参数算术正确，不能替代未来实现的实际参数统计、前向正确性、收敛性或 GPU 性能验证。

## 16. 本次完成状态

- [x] 核对永久记忆与仓库记忆，遵守仓库工具与范围约束。
- [x] 核查现有骨干、条件链、训练/LoRA、原生优化和 ONNX 接口。
- [x] 查阅一手论文/官方部署资料，标明读取范围与证据局限。
- [x] 给出具体默认结构、骨干/整模示例/LoRA 参数量及可复算公式。
- [x] 补充消费级 S/M/L/XL 与 B1/B2 容量档，单列参数、权重/优化器状态下限、微调/LoRA/推理资源和 B 级限制。
- [x] 计算显存下限、attention 张量与主要 FLOPs，给出有条件的 8GB 资源方案。
- [x] 给出单步推理、ONNX、GGUF 的可行性、限制和实施路线。
- [x] 写成实施与验证计划，并按阶段持续更新真实完成状态。
- [x] P0 冻结兼容基线与接口契约；新增 `configs/dit/` 级联配置、模板、最小矩阵及配置校验。
- [x] P1 浮点 DiT、训练链、严格 LoRA 和 8GB 合成稳定性验证。
- [x] P2 原生 PyTorch 推理、动态 mask、优化保护、生命周期与功能/资源回归。
- [ ] P3–P6 ONNX、量化、少步/单步蒸馏与 GGUF：尚未开始。

**最终建议**：先交付可回退的 22M 浮点 DiT + 8GB 微调/LoRA 档 + legacy ONNX 兼容，再以测量结果决定 INT8/INT4 默认；单步与 GGUF 独立推进。优先把兼容性、条件保真和真实资源峰值做正确，而不是以文件更小、参数更少或“设置成 1 步”替代完整工程验收。